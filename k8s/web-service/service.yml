apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: infraflow
spec:
  type: NodePort
  selector:
    app: web-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
