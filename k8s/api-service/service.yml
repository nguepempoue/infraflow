apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: infraflow
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - name: http
      port: 8080
      targetPort: http

